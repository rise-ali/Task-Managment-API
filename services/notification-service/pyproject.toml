[project]
name ="notification-service"
version"0.1.0"
description = "Task notification consumer service"
requires-python= ">3.12"
dependencies=[
    "aio-pika>=9.4.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires=["hatchling"]
build-backend = "hatchling.build"